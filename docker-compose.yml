version: '3.9'

services:
  vite-dev:
    container_name: myapp
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
    restart: always  # ← This makes it auto-start when Docker starts
